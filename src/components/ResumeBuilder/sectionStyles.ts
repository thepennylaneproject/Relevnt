/**
 * Resume Builder Section Styles
 * Uses design tokens from design-tokens.css
 * These classes are applied inline via className - prefer using the .input, .label CSS classes
 */

export const labelClass =
    'label';

export const helperClass =
    'helper-text';

export const inputClass =
    'input';

export const textareaClass = 'input min-h-[120px] resize-y';

export const addButtonClass =
    'btn btn--secondary btn--sm';

export const removeButtonClass =
    'inline-flex items-center gap-1.5 rounded-full border px-3 py-1.5 text-xs font-semibold cursor-pointer transition hover:-translate-y-[1px]'
    + ' border-[var(--color-error)] bg-[var(--color-error-bg)] text-[var(--color-error)] hover:opacity-90';

export const itemCardClass =
    'card';